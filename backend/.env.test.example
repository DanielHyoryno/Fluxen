NODE_ENV=test
JWT_SECRET=test-only-secret-do-not-use-in-production
BUSINESS_TIMEZONE=Asia/Jakarta

# Integration tests refuse to run unless the database name contains "test".
# Create an empty, disposable PostgreSQL database first.
TEST_DATABASE_URL=postgresql://skripsi_user:skripsi_pass@localhost:5432/skripsi_test
DB_SSL=false
